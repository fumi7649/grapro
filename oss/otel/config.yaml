receivers:
  otlp:
    protocols:
      grpc:
        

processors:
  batch:
    timeout: 5s

exporters:
  prometheus:
    endpoint: "0.0.0.0:8889"

# extensions:
#   health_check:
#     endpoint: "0.0.0.0:13133"

service:
  # extensions: [health_check]
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [prometheus]